<%- include header %>
This is mainpage aaaaaa
<%- include footer %>
DEBUG=poster:* npm start
DEBUG=blog:* npm run devstart
npm install --save mongoose
mongod --dbpath ‘/usr/local/mongodb/blog'

<nav>
	<span><a title="homepage" href="/">~ home ~</a></span>
	<% if (user) { %>
		<span><a title="post" href="/post">~ post ~</a></span>
		<span><a title="logout" href="/logout">~ logout ~</a></span>
	<% } else { %>
		<span><a title="login" href="/login">~ login ~</a></span>
		<span><a title="register" href="/reg">~ register ~</a></span>
	<% } %>
</nav>

		<p>
				<%- post.post %>
				<p class="info">
						<i class="fas fa-glasses"></i> Read: <%= post.pv %> | 
						<i class="far fa-comment-dots"></i> Comment : <%= post.comments.length %> | 
						<i class="fas fa-retweet"></i> Share:
						<% if (post.reprint_info.reprint_to) { %>
								<%= post.reprint_info.reprint_to.length %>
						<% } else { %>
								<%= 0 %>
						<% } %>
				</p>
		</p>

				<p class="info">
				<a href="/u/<%= post.name %>"><img src="<%= post.head %>" class="r_head" /></a>  | 

				User:
						<a href="/u/<%= post.name %>"><%= post.name %></a> | 
				Date: 
						<%= post.time.minute %> | 
				Tag:
						<% post.tags.forEach(function (tag, index) { %>
								<% if (tag) { %>
										<a class="tag" href="/tags/<%= tag %>"><%= tag %></a>
								<% } %>
						<% }) %>
				<% if (post.reprint_info.reprint_from) { %>
						<br>
						<a href="/u/<%= post.reprint_info.reprint_from.name %>/<%= post.reprint_info.reprint_from.day %>/<%= post.reprint_info.reprint_from.title %>">Original post</a>
				<% } %>
		</p>



<nav>
	<span>
		<a title="homepage" href="/"><i class="fas fa-home"></i></a>
	</span>
	<span>
		<a title="archive" href="/archive"><i class="far fa-folder-open"></i></a>
	</span>
	<span>
		<a title="tags" href="/tags"><i class="fas fa-hashtag"></i></a>
	</span>
	<% if (user) { %>
		<span>
			<form action="/search" method="GET">
				<i class="fas fa-search-plus"></i> <input type="text" name="keyword" placeholder="keyword" class="search" />
			</form>
		</span>
		<span>
				<a title="Link" href="/links"><i class="fas fa-link"></i></a>
		</span>
		<span>
			<a title="post" href="/post"><i class="fas fa-pen-square"></i></a>
		</span>
		<span>
			<a title="upload" href="/upload"><i class="fas fa-cloud-upload-alt"></i></a>
		</span>
		<span>
			<a title="logout" href="/logout"><i class="fas fa-sign-out-alt"></i></a>
		</span>
	<% } else { %>
		<span>
			<a title="login" href="/login"><i class="fas fa-sign-in-alt"></i></a>
		</span>
		<span>
			<a title="register" href="/reg"><i class="fas fa-user-plus"></i></a>
		</span>
	<% } %>
</nav>

    <p>
        <h2><a href="/u/<%= post.name %>/<%= post.time.day %>/<%= post.title %>"><%= post.title %></a></h2>
    </p>


    	<p>
		<h2><a href="u/<%= post.name %>/<%= post.time.day %>/<%= post.title %>"><%= post.title %></a></h2>
	</p>